# 편집자P 디자인 시스템

> 2026-09-23 확정. 구성은 세로 편집형(왼쪽 섹션 이름 + 오른쪽 본문), 팔레트는 흰 바탕 + 바로바로(남색·노랑).
> 이 문서가 디자인의 단일 출처다. CLAUDE.md와 코드 주석의 옛 설명(크림·코랄, 학습 서재 녹색)은 이 문서로 대체한다.

## 1. 원칙

편집자가 만든 사이트는 책처럼 보여야 한다. AI가 흉내 낼 수 없는 자산은 **실제 책, 실제 표지, 실제 강의 이력, 실제 사람**이다. 디자인은 이 자산을 드러내는 판면 역할만 한다.

- 사이트의 세 기둥은 **로드맵(배우기) · 서재(읽기) · 소개(만나기)** 이며 비중이 같다.
- 사이트 이름은 **편집자P의 AI 서재** 하나만 쓴다. "AI 강의·편집실", "AI 학습 로드맵", "학습 서재" 같은 옛 이름과 별칭을 쓰지 않는다.
- 헤더 왼쪽 위 로고는 아이콘 없이 글자로만 쓴다. 편집자P 캐릭터는 로고가 아니라 각 섹션에 녹아드는 삽화로 쓴다(아래 "캐릭터" 절).
- 기본 레이아웃 요소는 **가는 선으로 구분한 목록**이다. 책의 목차와 색인처럼 읽힌다. 카드는 쓰지 않는다. 표지와 영상 섬네일만 이미지 그대로 보여준다.
- 배경은 항상 흰색이다. 색은 책 표지에서 가져와 표지의 남색과 노랑 두 가지만 쓴다.

## 2. 금지 패턴

아래 패턴은 새 화면에서 쓰지 않는다. 에이전트가 작업할 때도 이 목록을 먼저 확인한다.

| 금지 | 대신 |
| --- | --- |
| 제목 위 작은 라벨(eyebrow), 알약 배지 | 필요한 정보면 제목 아래 메타 줄에 쓴다 |
| 두 줄 제목의 둘째 줄만 강조색 | 제목은 한 색. 강조는 굵기와 크기로 |
| 가운데 정렬 첫 화면 | 왼쪽 정렬 |
| "46권 · 34건" 같은 숫자 띠 | 숫자는 문장 안에서 맥락과 함께 |
| 섹션마다 h2 + 회색 부제 반복 | 섹션 이름표(명사)만. 설명이 필요한 곳에만 한 문장 |
| 마침표로 끝나는 광고 문장형 제목 | "로드맵", "서재", "강의 이력" 같은 명사형 |
| 모든 줄 끝의 화살표 아이콘 | 링크는 밑줄 또는 행 전체 호버로 표현. 외부 링크만 ↗ |
| 연한 배경색 패널·카드 반복, 옅은 그림자 | 가는 선과 여백 |
| 방사형·선형 그라데이션, 색 번짐, 글로 | 쓰지 않는다 |
| 카드 한쪽 가장자리 색 띠 | 쓰지 않는다 |
| 슬로건("배움을 쌓고, 나의 가능성을 넓히는 공간") | 사실만 쓴다 |
| 13px 미만 글자(코드 줄번호 제외) | 메타 정보도 14px 이상 |
| 인라인 `style={{}}` | CSS Modules |

## 3. 문구 규칙

- 사실만 쓴다. 경력·숫자는 `data/` 파일에서 계산한 값만 쓴다. 후기·리뷰를 만들지 않는다.
- 설명이 필요하면 편집자P의 1인칭으로 짧게 쓴다 ("책에 다 못 담은 내용을 영상으로 풉니다").
- 제목과 메뉴는 명사형. 버튼은 동작(`읽기`, `이어 읽기`, `강의 문의`).
- 한 화면에서 같은 뜻의 말을 두 번 하지 않는다.

## 4. 토큰

### 공통

- 글꼴: UI·본문 모두 **EditorP Sans**(Pretendard 파생, `public/fonts/`). 명조체는 쓰지 않는다. 코드는 시스템 고정폭 글꼴(`ui-monospace, SF Mono, Menlo, Consolas`)에 한글은 본문 글꼴로 이어 쓴다. 웹 글꼴을 추가로 싣지 않는다.
- 글자 크기: 14 / 16 / 17(본문 읽기) / 20 / 26 / 36 / 48px. 단계를 늘리지 않는다.
- 굵기: 본문 400, 강조 600, 제목 700.
- 모서리: 조작부 6px, 표지·섬네일 2px, 목록 0.
- 그림자: 책 표지에만 얇게 1단계.
- 콘텐츠 최대 너비 1200px, 모바일 좌우 여백 20px.

### 팔레트 — 바로바로 (흰 바탕 + 표지 남색 + 표지 노랑)

**배경은 항상 흰색이다.** 다크 모드, 세피아, 테마 전환은 두지 않는다. 남색을 배경으로 깔지 않는다.

《바로바로 파이썬》 표지에서 뽑은 남색 `#1F4292`, 노랑 `#EEA93B`만 쓴다. 노랑은 흰 바탕 대비가 1.98:1이라 **글자색으로 쓰지 않는다.** 역할을 나눈다.

- 남색: 글자 강조, 링크, 버튼, 현재 위치 글자, 연습·단계 번호.
- 노랑: 형광펜(글자 뒤 칠), 진행 막대, 로드맵 선, 완료 체크, 현재 위치 점, 선택 영역.

| 토큰 | 값 | 쓰임 |
| --- | --- | --- |
| paper | #FFFFFF | 배경 |
| ink | #0E1733 | 제목 |
| body | #2A3142 | 본문 |
| muted | #5D6577 | 메타 정보(흰 바탕 5.9:1) |
| rule | #E3E6ED | 구분선 |
| accent | #1F4292 | 링크·버튼·현재 위치(흰 바탕 9.2:1) |
| yellow | #EEA93B | 형광펜·진행 막대·선·완료 체크 |
| mark | 노랑 55% | 형광펜 칠 |
| code-bg | #F5F7FB | 코드 블록 |

버튼은 남색 바탕에 흰 글자, 누르면 `ink`로 짙어진다.

코드 구문 강조도 같은 팔레트의 전용 밝은 테마 하나만 쓴다: 키워드 남색 굵게, 문자열 황토 `#8A5A00`(흰 바탕 5.9:1), 주석 회색 기울임.

형광펜은 한 화면에 한두 곳만 쓴다(홈의 핵심 숫자, 리더의 연습 번호).

검토 후 제외한 안: 빨간펜(주홍 #C23B1A), 서재 녹색(#1E5A41), 남색 바탕 다크 모드, 세 입구 3단 홈 구성.

### 캐릭터

편집자P 캐릭터(검은 바가지 머리, 크림색 얼굴, 세로 타원 눈, 검은 후드티의 클레이 인형)를 포즈와 표정을 바꿔 사이트 곳곳에 둔다. 원본 6면도는 `assets/character/reference-sheet.png`, 생성 목록은 `assets/character/shots.json`, 생성은 `npm run character:generate`(OpenAI `gpt-image-2.5-sunburst`, 이미지 편집 API).

- 투명 배경 PNG로 만들고, 고른 최종본만 WebP로 바꿔 `public/character/`에 둔다.
- 입이나 눈썹을 새로 붙이지 않는다. 감정은 눈 모양, 고개 기울기, 몸짓으로만 표현한다.
- 소품은 클레이 질감으로, 색은 남색·노랑·흰색·크림만 쓴다. 이미지 안에 글자를 넣지 않는다.
- 한 덩어리(섹션 이름 열, 목록 하나, 문의 줄 등)에 최대 한 컷. 같은 화면에 같은 컷을 두 번 쓰지 않는다. 글자 뒤에 깔거나 글자를 가리지 않는다.
- 아래가 평평하게 잘린 포즈(책상에 기댄 자세)는 가는 구분선 위에 올려 선에 기대 앉은 것처럼 둔다.
- 크기 기준: 홈 소개 160px 안팎, 섹션 120px 안팎, 리더 완료 64px 안팎. 모바일에서는 줄인다.
- 리더 본문 안에는 넣지 않는다. 절을 끝까지 읽은 순간의 완료 표시에만 쓴다.
- 정보를 전달하지 않는 장식이므로 `alt=""`. 움직임은 섹션 등장 규칙만 따른다.

## 5. 페이지 구조

- **공통 헤더:** 편집자P의 AI 서재(글자 로고) · 로드맵 · 서재 · 질문 · 소개 · 로그인. 드롭다운과 테마 전환 버튼 없음. 전체 영상과 참여한 책은 모바일 메뉴와 푸터에서 연결한다.
- **홈:** 첫 화면 왼쪽에 1인칭 소개 문단, 오른쪽에 세 기둥(로드맵·서재·소개) 목차. 아래로 세 기둥 섹션이 같은 무게로 이어지고, 섹션 이름은 왼쪽 열에 붙어 따라온다. 방문 이력이 있으면 맨 위에 한 줄짜리 이어보기/이어 읽기.
- **로드맵:** 옛 시작점 선택(QuickStart)은 목록의 첫 줄 "처음이라면"으로 흡수했다. 공통 기초 → 문서·업무 / 웹·앱 제작 → 심화를 한 줄기로 보여준다. 도서 연계 로드맵(Firestore)은 그 아래 목록으로.
- **서재(`/books`, 홈 서재 섹션):** 직접 쓴 책(큰 표지, 역할·출판사) → 무료로 읽기(진도·이어 읽기) → 기획·편집으로 참여한 책(작은 표지, 직접 쓴 책 제외). 집필 가이드 예제는 공개 서가에서 뺀다(주소로는 열린다). `/edited-books`는 "참여한 책" 검색 화면으로 유지한다.
- **책 소개:** 왼쪽 표지와 메타(저자·공개일·분량·대상·선수 지식·관련 로드맵), 오른쪽 목차. 목차가 첫 화면에 보여야 한다.
- **소개:** 1인칭 사실 문장 → 대표 도서 → 강의 이력(대표 사례, 연도별 타임라인) → 프로필 → 강의 문의.

## 6. 리더

- 본문 폭 최대 **34em**, 17px, 행간 1.8, `word-break: keep-all`, 문단 간격 1em. 한글 단일 단 권장 글줄(글자 크기의 25~35배)을 따른다.
- 데스크톱: 왼쪽 책 목차(접기 가능) · 가운데 본문 · 오른쪽 "이 절에서"(1280px 이상).
- 모바일: 상단 한 줄 바(책 제목·목차 열기·설정), 목차는 아래에서 올라오는 시트.
- 읽기 설정: 글자 크기 3단계만. 브라우저에 저장.
- 진도: 마지막 절과 스크롤 위치, 절 완료. 서버로 보내지 않는다.
- 코드: 서버에서 구문 강조(Shiki), 복사 버튼, 언어 표시, 모바일은 가로 스크롤.
- 이미지: 가로·세로 크기와 alt를 원고에 기록한다. 본문 폭을 넘지 않고, 누르면 확대된다.
- 원고 구조: 실습 단계(01, 02…)는 번호 절차로, [Note]/[TIP]은 박스로.
- 이동: ←/→ 키로 이전·다음 절. 절 끝에 이전/다음, 관련 영상, "이 절에 질문하기".

## 7. 스크롤 인터랙션

스크롤이 정보를 주는 곳에만 쓴다.

| 위치 | 동작 |
| --- | --- |
| 리더 | 상단 진행 막대, 내려가면 헤더 숨김·올라가면 표시, "이 절에서" 현재 소제목 강조, 끝까지 읽으면 완료 체크와 다음 절 안내 |
| 로드맵 | 기초 → 갈래 → 심화를 잇는 세로선이 스크롤에 맞춰 채워짐 |
| 소개 강의 이력 | 연도 표시가 상단에 붙어 따라옴 |
| 섹션 진입 | 한 번만, 8~12px 떠오르며 등장(0.25초 안팎) |
| 공통 헤더 | 스크롤을 시작하면 아래 구분선 표시 |

규칙: 스크롤 재킹과 패럴랙스 금지. 위치 이동과 투명도 변화로만 움직인다. `prefers-reduced-motion`이면 전부 끈다. 가능하면 CSS 스크롤 연동 애니메이션을 쓰고, 지원하지 않는 브라우저는 정적으로 보인다.

## 8. 접근성·유지할 기능

- 본문 바로가기, 눈에 보이는 입력 레이블, 키보드 포커스, `aria-current`/`aria-pressed`/`aria-expanded`, 44px 이상 터치 영역.
- 테마는 흰 바탕 하나다. 기존 `ThemeToggle`과 `html[data-theme='dark']` 토큰은 제거하고 `color-scheme: light`로 고정한다.
- 레이어: 헤더 50, 메뉴 49, 에디터 툴바 30.
- 강의실(`/learn/[id]`)의 영상 크기·목차 규칙, 강의별 URL, 완료·되돌리기, 이어보기, 영상 검색의 URL 상태 보존은 유지한다.
- `/roadmaps/[id]`, `/books`, `/edited-books`, `/qna`, `/admin` 주소와 Firestore/파일 이중 저장 흐름은 유지한다.

## 9. 아직 옛 구조가 남은 화면

색·헤더·푸터는 새 체계를 따르지만 레이아웃은 아직 옛 구조다. 손볼 때 이 문서의 금지 패턴을 적용한다.

- 강의실 `/learn/[id]`: 카드형 목차, 알약 배지, 화살표 아이콘.
- 도서 연계 로드맵 `/roadmaps/[id]`, 전체 영상 `/videos`, 참여한 책 `/edited-books`: 카드와 연한 패널.
- 질문 게시판 `/qna` 하위 화면과 관리자 화면: 인라인 스타일.
